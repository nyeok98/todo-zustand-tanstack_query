const Header = () => {
  return (
    <h1
      style={{
        textAlign: "center",
        color: "black",
        margin: "var(--section-margin) 0",
      }}
    >
      TODO LIST
    </h1>
  );
};

export default Header;
